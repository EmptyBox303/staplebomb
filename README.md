# staplebomb
A web extension that keeps track of how long you spend on websites. Set "bombs" to remove tabs of a kind once a timer reaches 0.

# background processes
Background continuously monitors the current tab open and keeps track of the time spent in each tab's domain. Every minute, time tracking is "sealed" in a packet/data of something, 
